fastapi==0.115.0
uvicorn[standard]==0.30.6
SQLAlchemy==2.0.35
alembic==1.13.3
psycopg2-binary==2.9.12
pydantic==2.13.4
pydantic-settings==2.14.2
python-jose[cryptography]==3.3.0
python-dotenv==1.0.1
python-multipart==0.0.12
httpx==0.27.2

# PDF generation (payslips) + PDF text extraction (policy doc ingestion) -
# both use PyMuPDF (fitz), matching Module 3 / Module 5 of the proposal.
PyMuPDF==1.24.11

# RAG pipeline (Module 5): ChromaDB vector store + LangChain orchestration +
# Sentence-Transformers removed — embeddings now go through Gemini's API
# instead of a locally-loaded model (see rag_pipeline.py docstring for why:
# loading PyTorch + the model in-process exceeded available memory on a
# free-tier host). This also meaningfully shrinks the Docker image and
# speeds up every deploy, since sentence-transformers pulled in torch as a
# transitive dependency.
chromadb==0.5.15
langchain==0.3.7
langchain-chroma==0.1.4
langchain-google-genai==2.0.4
google-generativeai==0.8.3

# QA (Section 10 - mandatory)
pytest==8.3.3
pytest-cov==5.0.0
pytest-asyncio==0.24.0
